import * as buildClean from './build-clean.js';
import * as forReduxForm from './for-redux-form.js';

export const SimpleSchemaTransform = {
  buildClean,
  forReduxForm,
};

export default SimpleSchemaTransform;

